fastapi
uvicorn[standard]
sqlalchemy
python-jose[cryptography]
passlib[bcrypt]
python-dotenv
email-validator
groq
pytest
pytest-asyncio
httpx

# Optional: only needed if DATABASE_URL points to a PostgreSQL instance.
# Uncomment to enable. Default DB is SQLite (no extra driver required).
# psycopg2-binary
PyYAML
